{% extends '@distilled/layout/page-guest-split.html.twig' %}

{% block above_content %}
  <h2 class="mt-6 text-2xl font-bold leading-9 tracking-tight text-gray-900">Sign in to your account</h2>
  <p class="mt-2 text-sm leading-6 text-gray-500">
    Not a member?
    <a href="{{ url('user.register') }}" class="font-semibold text-slate-900 hover:text-slate-800">Sign up</a>
  </p>
{% endblock %}
