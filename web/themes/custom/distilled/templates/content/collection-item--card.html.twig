{#{{ dump() }}#}
<div class="col-span-1 flex flex-col divide-y divide-gray-200 rounded-lg bg-white text-left shadow relative">
  <div class="flex flex-1 flex-col">
    <img class="rounded-t-lg" src="https://placehold.co/600x400" alt="" />
    <div class="px-5 py-3">
      <div class="mb-2 text-2xl font-medium tracking-tight text-gray-900 dark:text-white">
        <a href="{{ url('entity.collection_item.canonical', {collection_item: collection_item.id}) }}">
          <span aria-hidden="true" class="absolute inset-0"></span>
          {{- collection_item.label -}}
        </a>
      </div>
    </div>
  </div>
  <div>
    <div class="-mt-px flex divide-x divide-gray-200">
      <div class="-ml-px flex w-0 flex-1">
        <a href="{{ url('entity.collection_item.canonical', {collection_item: collection_item.id}) }}" class="relative inline-flex w-0 flex-1 items-center justify-center gap-x-3 rounded-br-lg border border-transparent py-2 text-xs font-semibold text-gray-900">
          View
        </a>
      </div>
      <div class="-ml-px flex w-0 flex-1">
        <a href="{{ url('entity.collection_item.edit_form', {collection_item: collection_item.id}) }}" class="relative inline-flex w-0 flex-1 items-center justify-center gap-x-3 rounded-br-lg border border-transparent py-2 text-xs font-semibold text-gray-900">
          Edit
        </a>
      </div>
    </div>
  </div>
</div>

{#<div class="col-span-1 divide-y divide-gray-200 rounded-lg bg-white shadow">#}
{#  <div class="flex w-full items-center justify-between space-x-6 p-6">#}
{#    <div class="flex-1 truncate">#}
{#      <div class="flex items-center space-x-3 font-bold text-xl">{{ elements.name }}</div>#}
{#    </div>#}
{#  </div>#}
{#  <div>#}
{#    <div class="-mt-px flex divide-x divide-gray-200">#}
{#      <div class="flex w-0 flex-1">#}
{#        <a href="mailto:janecooper@example.com" class="relative -mr-px inline-flex w-0 flex-1 items-center justify-center gap-x-3 rounded-bl-lg border border-transparent py-2 text-sm font-semibold text-gray-900">#}
{#          <svg class="h-5 w-5 text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">#}
{#            <path d="M3 4a2 2 0 00-2 2v1.161l8.441 4.221a1.25 1.25 0 001.118 0L19 7.162V6a2 2 0 00-2-2H3z"></path>#}
{#            <path d="M19 8.839l-7.77 3.885a2.75 2.75 0 01-2.46 0L1 8.839V14a2 2 0 002 2h14a2 2 0 002-2V8.839z"></path>#}
{#          </svg>#}
{#          Email#}
{#        </a>#}
{#      </div>#}
{#    </div>#}
{#  </div>#}
{#</div>#}
