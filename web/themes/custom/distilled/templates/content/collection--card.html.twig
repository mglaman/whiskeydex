<div class="'relative rounded-lg border border-gray-300 bg-white px-3 py-2 shadow-sm space-y-3 hover:border-stone-400'">
  <div class="flex items-start">
    <div class="flex-grow">
      <div class="font-bold">{{ elements.name }}</div>
      <div class="text-cs">Count: {{ collection.getItemsCount }}</div>
    </div>
    <div class="flex-shrink">
      {{ elements.links }}
    </div>
  </div>
</div>
